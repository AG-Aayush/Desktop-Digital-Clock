# Tools needed to develop and package the app, on top of the runtime deps.
#     .venv\Scripts\python -m pip install -r requirements-dev.txt
-r requirements.txt
pyinstaller>=6.22.2
pytest>=8.0
pytest-qt>=4.4
ruff>=0.6
pre-commit
